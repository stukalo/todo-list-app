import React from 'react';
import PropTypes from 'prop-types';

const Footer = props => {
    const {
    } = props;

    return (
        <div className={'footer'}>
        </div>
    );
};

Footer.propTypes = {};

export default Footer;